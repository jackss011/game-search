import React from "react";
import ReactDOM from "react-dom";

const GameRow = ({ priority, thumbail, name }) => (
  <li className="">
    <img src="#" />
  </li>
);

function App() {
  return <div>Test App</div>;
}

ReactDOM.render(document.getElementById("root"), <App />);
